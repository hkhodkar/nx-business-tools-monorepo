import { DisplayScalesDirective } from './display-scales.directive';

describe('DisplayScalesDirective', () => {
  it('should create an instance', () => {
    const directive = new DisplayScalesDirective();
    expect(directive).toBeTruthy();
  });
});
